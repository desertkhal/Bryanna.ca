/* ============================================================
   Supabase connection for the Guestbook.
   ------------------------------------------------------------
   Josh: paste your two values between the quotes below, then
   save. That's the ONLY edit you need to make in this file.
   (Both values are safe to be public — that's how Supabase
   "anon" keys are designed. Row-level security protects data.)
   ============================================================ */
window.MUSEUM_CONFIG = {
  SUPABASE_URL: "PASTE_YOUR_PROJECT_URL_HERE",
  SUPABASE_ANON_KEY: "PASTE_YOUR_ANON_KEY_HERE"
};
